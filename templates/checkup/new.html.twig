{% extends 'base.html.twig' %}

{% block title %}Device Checkup{% endblock %}

{% block body %}
    {% include "_page_title.html.twig" with {'title': 'Device Checkup', 'main': 'Home', 'current': 'Device Checkup'} %}
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title mb-4">Room #{{ device.roomid.name}}: {{ device.name }}</h4>
                    <h6 class="card-subtitle font-14 text-muted">{{ 'now'| date('Y-m-d')}}</h6>
                     {{ include('checkup/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}