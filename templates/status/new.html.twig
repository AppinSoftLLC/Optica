{% extends 'base.html.twig' %}

{% block title %}New Status{% endblock %}

{% block body %}
    {% include "_page_title.html.twig" with {'title': 'Status', 'main': 'Setting', 'current': 'New Status'} %}
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title mb-4">New Status</h4>
                    {{ include('status/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}