{% extends 'base.html.twig' %}

{% block title %}Status index{% endblock %}

{% block body %}
    {% include "_page_title.html.twig" with {'title': 'Status', 'main': 'Setting', 'current': 'Status'} %}
	<div class="rows">
		<div class="col-xl-12">
			<div class="card">
				<div class="card-body">
					<div class="table-responsive">
						<h4 class="card-title">Status List</h4>
						<p class="card-title-desc">All status of device</p>
						<div class="table-responsive">
							<table class="table align-middle mb-0">
								<thead>
									<tr>
										<th>#</th>
										<th>Status</th>
										<th>Action</th>
									</tr>
								</thead>
								<tbody>
									{% for status in statuses %}
									<tr>
										<td style="width: 60px">{{ loop.index }}.</td>
										<td>{{ status.title }}</td>
										<td style="width: 160px">
											<div class="d-flex">
												<a class="btn btn-light btn-sm me-2" href="{{path('status_edit', {id: status.id})}}">Edit</a>
												{{ include('status/_delete_form.html.twig') }}
											</div>
										</td>
									</tr>
									{% else %}
										<tr>
											<td colspan="3">No records found</td>
										</tr>
									{% endfor %}
								</tbody>
							</table>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
    <div class="position-absolute" style="bottom: 80px; right: 24px;">
		<a class="btn btn-rounded btn-success waves-effect waves-light" href="{{ path('status_new') }}">
			<span class="mdi mdi-plus"></span>
		</a>
	</div>
{% endblock %}