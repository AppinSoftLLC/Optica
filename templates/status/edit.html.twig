{% extends 'base.html.twig' %}

{% block title %}Edit Status{% endblock %}

{% block body %}
    {% include "_page_title.html.twig" with {'title': 'Status', 'main': 'Setting', 'current': 'Edit Status'} %}
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title mb4">Edit Status</h4>
                    {{ include('status/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}