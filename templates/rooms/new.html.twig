{% extends 'base.html.twig' %}

{% block title %}New Rooms{% endblock %}

{% block body %}
    {% include "_page_title.html.twig" with {'title': 'Rooms', 'main': 'Setting', 'current': 'New Room'} %}
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title mb-4">New Room</h4>
                    {{ include('rooms/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}