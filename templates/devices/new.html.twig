{% extends 'base.html.twig' %}

{% block title %}New Device{% endblock %}

{% block body %}
    {% include "_page_title.html.twig" with {'title': 'Devices', 'main': 'Setting', 'current': 'New Device'} %}
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title mb-4">New Device</h4>
                    {{ include('devices/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}